import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#e8e6e6',
    },
    primary: {
      main: '#0D1B2A',
    },
  },
});

export default theme;
